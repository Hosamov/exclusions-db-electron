extends ../layout.pug 

block content 
  div.main-container
    h2.content-title Delete exclusion - Are you sure?
    p.centered Are you sure you would like to delete the exclusion for 
      strong #{exclusion.first_name} #{exclusion.last_name}
      | ? 
      p.centered 
        strong Caution: 
        | This action cannot be undone.
    .btn-container
      button.btn(onclick=`history.back()`) No - Go back
      button.btn.btn-red(onclick=`window.location.href='/users/${user}/delete_user'`) Yes - DELETE