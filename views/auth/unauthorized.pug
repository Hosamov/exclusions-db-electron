extends ../layout.pug 

block content  
  .main-container
    h3.content-title Unauthorized.
    p Please check with your administrator. It looks like you don't have the proper credentials to access that area.
    button.btn.btn-default(onclick="history.back()") Back
    button.btn(onclick="location.href='/'") Home